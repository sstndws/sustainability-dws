# Copy to `.env.local` for local dev. On Vercel → Project Settings → Environment Variables.
# NEVER commit `.env.local`.

# ── Login gate (default: OFF — dashboard langsung terbuka) ──
# Set true hanya jika mau pakai Supabase login lagi.
# VITE_AUTH_ENABLED=true

# ── Supabase (hanya jika VITE_AUTH_ENABLED=true) ──
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
# Set false untuk dev lokal langsung ke GAS.
# VITE_SECURE_GAS=false

# ── Server-only di Vercel (tanpa prefix VITE_) ──
GAS_WEBAPP_URL=https://script.google.com/macros/s/AKfycbwaamq4kVc5oB7p7i7vZsmnl0GGjYdXbJ3EE3vL7INOisA-xKVRUp5naG4gVfi2fofT/exec

# Opsional — sama dengan API_SECRET di GAS Script Properties (aktifkan proteksi penuh)
# GAS_API_SECRET=
