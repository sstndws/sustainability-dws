{
  "name": "sustain-dashboard",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite --force",
    "dev:clean": "rm -rf node_modules/.vite && vite --force",
    "build": "vite build",
    "preview": "vite preview",
    "test:supply": "node scripts/test-supply-merge.mjs && node scripts/test-supply-routing.mjs && node scripts/test-mill-registry-views.mjs",
    "test:ttp": "node scripts/test-ttp-mill-sync.mjs",
    "test:period": "node scripts/test-period-filtering.mjs",
    "test:all": "npm run test:supply && npm run test:ttp && npm run test:period && npm run build"
  },
  "devDependencies": {
    "vite": "^5.4.0"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.105.4",
    "chart.js": "^4.5.1",
    "docx-preview": "^0.4.0",
    "html2canvas": "^1.4.1",
    "jspdf": "^2.5.1",
    "jspdf-autotable": "^3.8.2",
    "pizzip": "^3.2.0",
    "xlsx-js-style": "^1.2.0"
  }
}
